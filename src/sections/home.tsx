import Hero from '../components/hero';
import Projects from '../sections/projects';

const Home = () => {
    return (
        <div>
            <Hero />
            <Projects />
        </div>
    );
};

export default Home;
